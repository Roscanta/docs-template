# Getting Started

This folder is an example of an accordion group: it has its own `_meta.json` and
several sibling pages (**Overview**, **Quickstart**, **Authentication**), so Nextra
renders it as a collapsible section in the sidebar automatically — no extra
configuration beyond adding files and listing them here.
