# Guides

Everything that isn't an API reference page lives here — quickstarts, provider setup,
authentication, migration notes. Plain MDX, no generation involved.

Pick a topic from the sidebar to get started. Folders with multiple pages (like
**Getting Started** below) collapse into an accordion automatically — just add more
`.mdx` files and list them in that folder's `_meta.json`.
